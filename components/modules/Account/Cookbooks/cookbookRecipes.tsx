import React from "react";
import { Text } from "react-native";
import RecipesFlatList from "./recipesFlatList";

type CookbookRecipesProps = {
  recipes: string[];
  cookbookId: number;
};

const CookbookRecipes = ({ recipes,cookbookId }: CookbookRecipesProps) => {
    
};

export default CookbookRecipes;
